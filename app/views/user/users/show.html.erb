  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-9">
        <h3>マイページ</h3>
        <table class="table">
          <tbody>
            <tr>
              <%= image_tag @user.get_profile_image(200, 200) %>
            </tr>
            <tr>
              <th>ユーザ名</th>
              <th><%= @user.name %></th>
            </tr>
            <tr>
              <th>自己紹介</th>
              <th><%= @user.introduction %></th>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <%= link_to "プロフィールを編集", information_edit_path(@user), class: 'btn btn-outline-info rounded-pill btn-sm col-lg-9' %>